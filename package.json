{
  "name": "ept-assignment-photo-gallery",
  "author": {
    "name": "Peter Banjo"
  },
  "description": "Full-stack JavaScript app on AWS",
  "engines": {
    "node": ">=18"
  },
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docker:setup": "docker compose -f docker-compose.yml up --build --force-recreate",
    "docker:destroy": "docker compose down -v"
  }
}
